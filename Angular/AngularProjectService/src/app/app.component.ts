import { Component } from '@angular/core';
import { CalculatorService } from './calculator.service';
import { FactorialService } from './factorial.service';

@Component({
  selector: 'app-root',
  templateUrl: './app.component.html',
  styleUrl: './app.component.css'
})
export class AppComponent {
 /* sum:number
  sub:number
  factorials:number[]=[];
  input:number=0;
 
constructor(private cal:CalculatorService,private fact:FactorialService)  //injecting the service
 { 
  this.sum=cal.getAddition(10,20)
  this.sub=cal.getSubraction(50,30)

 }
 getFactorailValues(){
  this.factorials=this.fact.getFactorialValues(this.input);
 }
  */
 
 
}
