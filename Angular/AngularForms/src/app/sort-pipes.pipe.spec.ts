import { SortPipesPipe } from './sort-pipes.pipe';

describe('SortPipesPipe', () => {
  it('create an instance', () => {
    const pipe = new SortPipesPipe();
    expect(pipe).toBeTruthy();
  });
});
